# ============================================================
# Meitheal — Environment Variables
# Copy this file to .env.local and fill in your values.
# ============================================================

# Supabase — from supabase.com/dashboard → Project Settings → API
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
SUPABASE_SERVICE_KEY=your-service-role-key

# Supabase Access Token — from supabase.com/dashboard/account/tokens
# Used to run database migrations automatically during setup. Can be revoked afterwards.
SUPABASE_ACCESS_TOKEN=your-supabase-access-token

# GitHub — from github.com/settings/tokens/new (repo scope, no expiry)
GITHUB_TOKEN=your-github-personal-access-token
GITHUB_REPO=your-github-username/my-community
GITHUB_BRANCH=main

# AI provider — get from your chosen provider (see setup guide)
AI_API_KEY=your-ai-api-key
